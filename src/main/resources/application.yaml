spring:
  datasource:
    url: jdbc:mysql://localhost:3306/toyproject?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: jayho
    password: jayho1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
